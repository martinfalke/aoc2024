defmodule DayX do
  import Utils
  defp day(), do: X

  # Part 1
  def run_p() do
    lines = init_day(day())
    lines
  end

  # Part 2
  def run_q() do
    lines = init_day(day())
    lines
  end

end
